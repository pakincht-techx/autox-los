"use client";

import React from "react";

export default function GuarantorsLayout({ children }: { children: React.ReactNode }) {
    // Save draft button is now visible by default for all guarantor modules
    return <>{children}</>;
}
